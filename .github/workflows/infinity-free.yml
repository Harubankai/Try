name: Deploy to krustykrabb.kesug.com

# Run manually or on push to the main branch
on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy to krustykrabb.kesug.com
    runs-on: ubuntu-latest

    steps:
      # Step 1: Clone the latest code from GitHub
      - name: Get latest code
        uses: actions/checkout@v4

      # Step 2: Upload code via FTP
      - name: Deploy to InfinityFree via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ftpupload.net
          username: ${{if0_42021766 }}
          password: ${{ 8f9TjraiHM }}
          server-dir: /krustykrabb.kesug.com/htdocs  # For addon domains
          timeout: 86400000
          log-level: standard
